extends layout
block append css
    link(rel='stylesheet', href='/sheets/neumorph.css')
block content
    .align-self-center.neumorph.px-5.py-4
        h1.fs-1.mt-2.mb-3 Passkeys verwalten
        include errorWarning
        a.btn.btn-primary.m-1(href="/passkeys/register") Neuen Passkey registrieren
        br
        if credentials.length == 0
            p Keine Passkeys registriert.
        else
            ul.list-group.mt-3
                each cred in credentials
                    li.list-group-item.d-flex.justify-content-between.align-items-center
                        .me-3
                            strong= cred.name ? cred.name : cred.id
                            br
                            small.text-muted= cred.id
                        form(method="post", action="/passkeys/" + encodeURIComponent(cred.id) + "/delete")
                            button.btn.btn-outline-danger(type="submit") Löschen

*** End Patch
