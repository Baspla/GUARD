doctype html
html.h-100
    head
        meta(charset="utf-8")
        meta(name="title" content="GUARD SSO")
        meta(name="description" content="Der Gotteslachs Universal Authentfikations- und Registrierungs-Dienst")

        <!-- Open Graph / Facebook -->
        meta(property="og:type" content="website")
        meta(property="og:url" content="https://guard.timmorgner.de/")
        meta(property="og:title" content="GUARD SSO")
        meta(property="og:description" content="Single Sign On für die Gotteslachsbande")
        meta(property="og:image" content="https://guard.timmorgner.de/images/preview.png")

        <!-- Twitter -->
            meta(property="twitter:card" content="summary_large_image")
            meta(property="twitter:url" content="https://guard.timmorgner.de/")
            meta(property="twitter:title" content="GUARD SSO")
            meta(property="twitter:description" content="Single Sign On für die Gotteslachsbande")
            meta(property="twitter:image" content="https://guard.timmorgner.de/images/preview.png")
        title GUARD SSO
            if title != null
                |  | #{title}

        block css
        link(rel='stylesheet', href='/css/bootstrap.css')
        block scripts
            script(src='/js/bootstrap.js')
    body.h-100
        .text-center.h-100.d-flex.justify-content-center
            block content